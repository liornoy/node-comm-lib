package nodecommlib

import "testing"

func TestComm(t *testing.T) {
	// 1) get the output of the commands (on each node): ss -plant | grep -v “127.0” and  ss -planu | grep -v “127.0”
	// 2) get the comm matrix
	// 3) compare
}
